# Add a social preview image

## Problem

Sharing a link to the site produces no preview card: there is no image at
`site/static/og.png`, and `head.html` references none.

## Outcome

A social preview image exists at `site/static/og.png`, referenced from
`head.html`, once the site's visual direction is settled.
